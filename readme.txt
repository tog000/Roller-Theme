TODO

- Modularize the admin panel, so components can be defined somewhere and they just take the values.
- Right now, page.php invokes home.php. Find a mechanism to render individual pages (functions.php?)
